export enum headerK {
  XAPI_KEY = "x-apikey",
}
export enum apiCall {
  FLIGHT_SEARCH = "/api/flightData",
}
export enum identType {
  DESIGNATOR = "identType=designator",
}
export enum httpMethod {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  PATCH = "PATCH",
  DELETE = "DELETE",
}
export enum fallback {
  FL_DESIGNATOR = "DAL771",
}

export enum misc {
  dayInMs = 86400000,
}
